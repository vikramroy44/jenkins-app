server:
  port: 8086

spring:
  application:
    name: Config-Server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/vikramroy44/microservice-config
          clone-on-start: true
